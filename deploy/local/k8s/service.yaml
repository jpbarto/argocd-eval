apiVersion: v1
kind: Service
metadata:
  name: argo-eval-app
  labels:
    app: argo-eval-app
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 3000
  selector:
    app: argo-eval-app
